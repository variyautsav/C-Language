// with return with peremeter
// interst

#include<stdio.h>
int intreset(int p)
{
    return p*0.1;
}
int main()
{
    int p, ins;
    int intreset(int)

        printf("entre the price");
        scanf("%d",&p);

    printf(" tota intreset %d", intreset(p));
}
