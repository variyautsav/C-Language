// with perameter no return
// squear
#include<stdio.h>
float square(float l)
{
    printf("%f",l*l);
}
int main()
{
    float l;
    float square(float);
    printf("entre the lanth");
    scanf("%f",&l);

    square(l);
    return 0;
}
